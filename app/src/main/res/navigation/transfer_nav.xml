<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/transfer_nav"
    app:startDestination="@id/transferFragment">

    <fragment
        android:id="@+id/transferFragment"
        android:name="com.mkrs.kolt.presentation.transfer.TransferFragment"
        android:label="fragment_transfer"
        tools:layout="@layout/fragment_transfer" >
        <action
            android:id="@+id/action_transferFragment_to_bottomSheetTransferConfirmation"
            app:destination="@id/bottomSheetTransferConfirmation" />
    </fragment>
    <fragment
        android:id="@+id/bottomSheetTransferConfirmation"
        android:name="com.mkrs.kolt.presentation.transfer.BottomSheetTransferConfirmation"
        android:label="bottom_sheet_transfer_confirmation"
        tools:layout="@layout/bottom_sheet_transfer_confirmation" />
</navigation>